version: "3.8"

networks:
  rental-network:
    driver: bridge

services:
  # api:
  #   depends_on:
  #     - db
  #   build: .
  #   image: book_rental:0.1
  #   env_file:
  #     - .env
  #   ports:
  #     - "8000:8000"
  #   networks:
  #     - rental-network
  #   volumes:
  #     - ./:/usr/src/app
  #   command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload

  db:
    image: postgres
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=fastapi
    ports:
      - "5432:5432"
    networks:
      - rental-network
    volumes:
      - db:/var/lib/postgresql/data

volumes:
  db:
    driver: local
